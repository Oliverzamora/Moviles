<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauizAppUtn.Menu"
             Title="Menu">
    <Grid BackgroundColor="#f8f9fa">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackLayout Grid.Row="0" Padding="20" Spacing="20" HorizontalOptions="Center" VerticalOptions="Center">
            <Button Text="Clasificaciones" Clicked="Option1_Clicked" BackgroundColor="#007BFF" TextColor="White" CornerRadius="10" HeightRequest="50" WidthRequest="200"/>
            <Button Text="Productos" Clicked="Option2_Clicked" BackgroundColor="#007BFF" TextColor="White" CornerRadius="10" HeightRequest="50" WidthRequest="200"/>
        </StackLayout>

        <BoxView Grid.Row="1" BackgroundColor="#f8f9fa"/>
    </Grid>
</ContentPage>
